<div class="Event">
  <h1 class="Event-Title">{{title}}</h1>
  <div class="Event-Meta">
    {{# if area}}
      {{area}},
    {{/if}}
    {{type}}
  </div>
  <div class="Event-Description">
    <p>
      {{description}}
      {{#ifCond language "||" arranger}}
      <span class="Event-Description__arrangement">
        Arrangeras
        {{#if language}}på {{language}}{{/if}}{{#if arranger}} av {{arranger}}{{/if}}.
      </span>
      {{/ifCond}}
    </p>
  </div>
  <div class="Event-Details">
    <div>
      {{#each tags}}{{this}}, {{/each}}<br><br>
    </div>
    <div class="Event-Location">
      {{location}}
    </div>
    {{#each times}}
      <div class="Event-Time">
        {{{clockIcon start}}}
        {{prettyTimeSpan start end}}
      </div>
    {{/each}}
  </div>
</div>
